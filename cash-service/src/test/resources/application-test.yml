spring:
  application:
    name: cash-service
  config:
    activate:
      on-profile: test
  cloud:
    config:
      enabled: false
    consul:
      enabled: false
    discovery:
      enabled: false

  security:
    oauth2:
      resource-server:
        jwt:
          issuer-uri: http://localhost:8085/realms/bank-realm
      client:
        registration:
          account-service:
            provider: keycloak
            client-id: account-service
            client-secret: fpbcLLRoXyEJolIXEfqMiRMsevqXZnXG
            authorization-grant-type: client_credentials
        provider:
          keycloak:
            issuer-uri: http://localhost:8085/realms/bank-realm

bank:
  accounts-service:
    base-url: http://localhost:8081
  notifications-service:
    base-url: http://localhost:8086

server:
  port: 8084