apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cash.fullname" . }}-config
  labels:
    app.kubernetes.io/name: {{ include "cash.fullname" . }}
    app.kubernetes.io/component: backend
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}