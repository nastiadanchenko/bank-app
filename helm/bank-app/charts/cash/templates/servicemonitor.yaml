apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "cash.fullname" . }}-monitor
  labels:
    release: kube-prometheus-stack
    {{- include "cash.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      app: cash-service
      {{- include "cash.selectorLabels" . | nindent 4 }}
  namespaceSelector:
    matchNames:
      - default
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s