apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "notifications.fullname" . }}-config
  labels:
    app.kubernetes.io/name: {{ include "notifications.fullname" . }}
    app.kubernetes.io/component: backend
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
