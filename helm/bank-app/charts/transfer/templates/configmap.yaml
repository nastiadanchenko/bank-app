apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "transfer.fullname" . }}-config
  labels:
    app.kubernetes.io/name: {{ include "transfer.fullname" . }}
    app.kubernetes.io/component: backend
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}