rootProject.name = 'bank-app'

include(
        'config-server',
        'api-gateway',
        'front-ui',
        'accounts-service',
        'cash-service',
        'transfer-service',
        'notifications-service',
)
