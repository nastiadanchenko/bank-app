rootProject.name = 'bank-app'

include(
        'api-contract',
        'shared-kafka',
        'front-ui',
        'accounts-service',
        'cash-service',
        'transfer-service',
        'notifications-service',
)
